services:
  tiktok-stream-relay:
    build:
      context: .
    container_name: tiktok-stream-relay-local
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
    volumes:
      - ./cache:/app/cache
      - ./config:/app/config
